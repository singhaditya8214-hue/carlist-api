# Data Directory

This directory stores scraped car listings temporarily during GitHub Actions workflow execution.

The file `unified_cars_data.json` contains all scraped car listings in JSON format.
